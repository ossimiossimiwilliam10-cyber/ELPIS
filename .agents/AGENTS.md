# R√®gles de projet pour ELPIS

- Pour ce projet syst√©matiquement, √† la fin de chaque t√¢che ou de chaque modification importante, il faut **pousser les modifications sur Github** (`git add .`, `git commit -m "..."`, `git push`).
- Ensuite, il faut syst√©matiquement **compiler les modifications** pour l'application en ligne et locale (par exemple en ex√©cutant `npm run build` dans le dossier `interface/web`).
- L'objectif est d'assurer que l'utilisateur dispose toujours de la version la plus r√©cente et fonctionnelle √† la fois en ligne et en local.
- **Cr√©ation de tests anti-r√©gression** : Syst√©matiquement, lorsque tu vas rep√©rer et corriger un bug, **ou d√©velopper une nouvelle fonctionnalit√© / un nouveau composant**, il faudra au final cr√©er ou mettre √† jour les tests unitaires (anti-r√©gression) pour garantir une couverture de code continue et pr√©venir la r√©apparition de bugs.
- **Performance React & Zustand** : Ne jamais placer d'√©tats √† haute fr√©quence de mise √† jour (ex: chronom√®tre, compteurs rapides, positionnement souris) dans le store global (`useStore`). Ils doivent imp√©rativement √™tre isol√©s dans leur propre store Zustand d√©di√© (ex: `useChronoStore`) pour √©viter des re-rendus massifs et non d√©sir√©s sur l'ensemble de l'application.
- **Time-Awareness (Conscience du Temps R√©el)** : Ne jamais utiliser de r√©partitions temporelles statiques ou purement s√©quentielles pour la planification de t√¢ches (ex: "Matin", "Apr√®s-midi", "Soir"). Les algorithmes d'orchestration et les interfaces utilisateur doivent imp√©rativement v√©rifier l'heure locale actuelle (`new Date().getHours()`) pour assigner ou filtrer dynamiquement ces √©tiquettes. Si l'utilisateur se connecte √† 15h, le syst√®me ne doit plus proposer de t√¢ches pour le "Matin".
- **Moyenne Universitaire ECTS** : Toujours utiliser une moyenne pond√©r√©e par les cr√©dits ECTS pour calculer les moyennes globales (semestres, ann√©es, ou cycle entier), conform√©ment au standard LMD europ√©en. Ne jamais utiliser de moyenne arithm√©tique simple entre les semestres ou les ann√©es pour un score global.
- **Mise √† jour de la Streak (Gamification)** : La streak (jours cons√©cutifs d'√©tude) ne doit s'incr√©menter QUE lorsqu'une activit√© est r√©ellement effectu√©e et ajout√©e √† l'historique de l'utilisateur. L'ouverture de l'application ne doit jamais incr√©menter la streak, mais doit seulement servir √† v√©rifier si le joueur l'a perdue (plus d'un jour d'absence) afin de la remettre √† 0.
- **Archivage Granulaire & Automatique** : L'historique d'un √©tudiant doit pouvoir √™tre archiv√© de fa√ßon granulaire (ex: `s.archived` au niveau du semestre, ou `l.archived` au niveau de la licence) pour ne pas fausser le calcul de la Cible IA ni l'Orchestrateur. De plus, chaque semestre peut poss√©der une date de fin (`s.dateFin`) permettant au syst√®me de l'ignorer automatiquement une fois cette date d√©pass√©e, sans intervention manuelle de l'utilisateur.

- **SystËme de Toast (Notifications)** : Le hook `useToast()` retourne un objet, pas une fonction. Ne **jamais** appeler `toast("message", "type")` directement. Il faut impÈrativement utiliser ses mÈthodes spÈcifiques : `toast.success("...")`, `toast.error("...")`, `toast.info("...")`, ou `toast.warning("...")`.

- **Validation des chemins de fichiers (Anti-Path-Traversal)** : Ne jamais utiliser directement un chemin de fichier fourni par l'utilisateur (req.body, req.query, etc.) pour des opÈrations fs ou child_process. Toujours utiliser path.resolve() pour obtenir le chemin absolu et vÈrifier strictement qu'il commence par le rÈpertoire de base autorisÈ en utilisant resolvedPath.startsWith(allowedDir). Si le chemin sort du pÈrimËtre, renvoyer immÈdiatement une erreur HTTP 403.

- **Rejet des doublons ‡ l'upload** : Ne jamais configurer de systËme d'upload de fichiers (ex: via multer) qui Ècrase silencieusement un fichier existant de mÍme nom. Avant d'Ècrire le fichier sur le disque, vÈrifier systÈmatiquement avec fs.existsSync() si le fichier de destination existe dÈj‡. Si le fichier existe, stopper l'opÈration et renvoyer une erreur explicite au client.

- **Validation dans Multer** : Ne jamais utiliser cb(new Error(...)) ‡ l'intÈrieur des fonctions destination ou filename de multer.diskStorage pour des rËgles mÈtier (comme "le fichier existe dÈj‡"). Laisser multer uploader tous les fichiers dans un dossier temporaire et effectuer les vÈrifications mÈtier dans le contrÙleur de la route.

- **Transparence des erreurs** : Le Global Error Handler du backend doit toujours exposer le message d'erreur rÈel (err.message) au frontend dans le champ principal 'error' renvoyÈ en JSON. Ne jamais masquer une erreur mÈtier interceptÈe derriËre un message gÈnÈrique statique.
