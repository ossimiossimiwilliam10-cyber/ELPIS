import { describe, it, expect, vi, beforeEach } from 'vitest';
import { generate_dataset } from './generate_dataset';

describe('generate_dataset', () => {
  describe('generate_dataset', () => {
    it('should be defined', () => {
      expect(generate_dataset).toBeDefined();
    });

    it('should be a function', () => {
      // TODO: Vérifier le type exact (fonction, objet, classe...)
      // expect(typeof generate_dataset).toBe('function');
      expect(generate_dataset).toBeDefined();
    });
  });

});
