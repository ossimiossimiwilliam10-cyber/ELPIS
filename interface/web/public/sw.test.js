import { describe, it, expect, vi, beforeEach } from 'vitest';
import { sw } from './sw';

describe('sw', () => {
  describe('sw', () => {
    it('should be defined', () => {
      expect(sw).toBeDefined();
    });

    it('should be a function', () => {
      // TODO: Vérifier le type exact (fonction, objet, classe...)
      // expect(typeof sw).toBe('function');
      expect(sw).toBeDefined();
    });
  });

});
